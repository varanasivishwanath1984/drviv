export class DriverVehicleAssignement {
    driverLicenseNumber: string;
    registrationNumber: string;

    constructor(driverLiscenseNumber: string, registrationNumber: string) {
        this.driverLicenseNumber = driverLiscenseNumber;
        this.registrationNumber = registrationNumber;
      }
  }